# Optional integrations - uncomment and fill in to enable features
# All app config defaults are in src/config.py

# Override LLM model (default: qwen2.5:7b)
# LLM_MODEL=qwen2.5:7b

# Production JWT secret (required for production, auto-validated)
# Generate with: openssl rand -hex 32
# JWT_SECRET=your-secret-here

# Twilio - enables SMS and voice call reminders for tasks
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=

# VAPID - enables web push notifications
# Generate keys with: npx web-push generate-vapid-keys
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_EMAIL=mailto:you@example.com

# USDA FoodData Central - enables nutrition data lookup (free API)
# Get key at: https://fdc.nal.usda.gov/api-key-signup.html
# USDA_API_KEY=
